{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug client",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "run client"
    }
  ]
}
